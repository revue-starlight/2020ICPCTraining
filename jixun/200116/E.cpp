/*
多组大法好，memset保平安
人在做，天在看，不加EOF留祸患
WA一出天地灭，赶紧多组报平安。
*/
#include <bits/stdc++.h>
using namespace std;
int k,m,n,p;
int a[2000001];
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin>>k>>m>>n>>p;
    for (int i=1;i<=n;i++){
        cin>>a[i];
    }
    sort(a+1,a+1+k);
     
}