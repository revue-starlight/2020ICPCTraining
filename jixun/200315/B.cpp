#include <bits/stdc++.h>
using namespace std;

int main() {
    int CASE;
    cin>>CASE;
    while (CASE--) {
        int n;
        scanf("%d", &n);
        int k=n;
        k--;
        while (k--) {
            int a,b;
            cin>>a>>b;
        }
        if (n%2==1) cout<<"Alice"<<endl;
        else cout<<"Bob"<<endl;
    }
}