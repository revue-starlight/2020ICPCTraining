#include <bits/stdc++.h>
using namespace std;
void solve(){
    int n,k;
    bool vis[1001];
    memset(vis,0,sizeof(vis));
    vector <int> vec[1001];
    cin>>n>>k;
    for (int i=1;i<=k;i++){
        int c;
        vec[i].clear();
        while (c--){
            int x; cin>>x;
            vec[i].push_back();
        }
        vec[0].clear();
    }
}

int main(){
    int t;
    cin>>t;
    while (t--){
        solve();
    }
}